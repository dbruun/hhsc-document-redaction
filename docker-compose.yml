version: '3.9'

services:
  backend:
    build:
      context: ./backend/DocumentRedaction.API
      dockerfile: Dockerfile
    ports:
      - '5000:8080'
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Azure__DocumentIntelligence__Endpoint=${AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT}
      - Azure__DocumentIntelligence__Key=${AZURE_DOCUMENT_INTELLIGENCE_KEY}
      - Azure__Language__Endpoint=${AZURE_LANGUAGE_ENDPOINT}
      - Azure__Language__Key=${AZURE_LANGUAGE_KEY}
      - Azure__Storage__ConnectionString=${AZURE_STORAGE_CONNECTION_STRING}
      - Azure__Storage__ContainerName=${AZURE_STORAGE_CONTAINER_NAME:-documents}
      - AllowedOrigins__0=http://localhost:5173
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '5173:80'
    depends_on:
      - backend
    restart: unless-stopped
